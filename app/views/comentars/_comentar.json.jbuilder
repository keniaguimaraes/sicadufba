json.extract! comentar, :id, :user_id, :disciplinacurso_id, :professor_id, :semestre_id,:comentario, :data_comentario,:denuncia,:bloqueio,:data_bloqueio,:created_at, :updated_at
json.url comentar_url(comentar, format: :json)
