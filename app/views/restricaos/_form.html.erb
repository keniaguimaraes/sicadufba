<%= form_for(restricao) do |f| %>
  <% if restricao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restricao.errors.count, "error") %> prohibited this restricao from being saved:</h2>

      <ul>
      <% restricao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
   <div class="row">
    <div class="col s12 m8 lg9 offset-m2">
     
 <div class="card-panel ">

   <div class="row">
      <div class="col s12 m12 lg12"> 
  <b><%= f.label :palavra %></b>
   <%= f.text_field :palavra %>
</div>
</div>
 
   <div class="row">
      <div class="col s12 m12 lg12"> 
    <%= f.submit "OK", class: 'btn waves-effect waves-light light-blue btn-small white-text' %>
  </div>

</div>   
</div>  
</div>    
<% end %>





